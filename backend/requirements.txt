# Backend dependencies. Install: pip install -r requirements.txt
# Versions are pinned so a reinstall after the break gives exactly the same thing.

fastapi==0.141.1
uvicorn[standard]==0.52.4
psycopg[binary]==3.3.5
python-dotenv==1.2.3
